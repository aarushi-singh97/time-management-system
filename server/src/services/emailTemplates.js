const escapeHtml = (value = '') => String(value).replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character]);
function template(title, message, details = []) { return `<main style="font-family:Arial,sans-serif;max-width:600px;margin:auto;color:#1e293b"><h2 style="color:#1d4ed8">${escapeHtml(title)}</h2><p>${escapeHtml(message)}</p>${details.map(([label, value]) => `<p><strong>${escapeHtml(label)}:</strong> ${escapeHtml(value)}</p>`).join('')}<hr><small>Time Management System</small></main>`; }
const appointmentEmail = (appointment, action) => ({ subject: `Appointment ${action}: ${appointment.title}`, html: template(`Appointment ${action}`, `Your appointment has been ${action.toLowerCase()}.`, [['Date', appointment.appointment_date], ['Time', `${appointment.start_time} - ${appointment.end_time}`], ['Venue', appointment.venue || 'Not specified']]) });
const meetingEmail = (meeting, action) => ({ subject: `Meeting ${action}: ${meeting.title}`, html: template(`Meeting ${action}`, action === 'Invitation' ? 'You have been invited to a meeting.' : `This meeting has been ${action.toLowerCase()}.`, [['Date', meeting.meeting_date], ['Time', `${meeting.start_time} - ${meeting.end_time}`], ['Venue', meeting.venue || 'Not specified']]) });
const leaveEmail = (leave, action) => ({ subject: `Leave request ${action}`, html: template(`Leave request ${action}`, `Your leave request has been ${action.toLowerCase()}.`, [['Dates', `${leave.start_date} to ${leave.end_date}`], ['Reason', leave.reason || 'Not specified']]) });
const reminderEmail = (items) => ({ subject: 'Today\'s schedule reminder', html: template('Today\'s schedule', 'Here are your upcoming items.', items.map((item) => [item.type, `${item.title} at ${item.time}`])) });
const summaryEmail = (summary, label) => ({ subject: `${label} Time Management summary`, html: template(`${label} summary`, 'Your current schedule overview.', Object.entries(summary)) });
module.exports = { appointmentEmail, meetingEmail, leaveEmail, reminderEmail, summaryEmail };
